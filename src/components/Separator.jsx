/**
 * Componente que renderiza una línea horizontal para separar secciones de contenido.
 * * Se utiliza para crear una división visual clara entre diferentes partes de una vista.
 *
 * @returns {React.ReactElement} El elemento JSX renderizado.
 */
export function Separator () {
  return <hr className='separator' />
}
